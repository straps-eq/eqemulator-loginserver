global:
  scrape_interval: 60s
  evaluation_interval: 60s

scrape_configs:
  - job_name: "eqemu-web"
    metrics_path: "/api/metrics"
    static_configs:
      - targets: ["nginx:80"]

  # Prometheus self-monitoring
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]
